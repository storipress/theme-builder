export { normalizeStyle, normalizeStyles, pluckBreakpoint } from 'shared/code-generator/style-tree'

export const PREVIEW_TO_BREAKPOINT = { mobile: 'xs', tablet: 'md', desktop: 'lg', expanded: 'lg' } as const
